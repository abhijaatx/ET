{
	email {$ACME_EMAIL}
}

{$SITE_ADDRESS} {
	encode zstd gzip

	@api path /api/* /health
	handle @api {
		reverse_proxy api:3001
	}

	handle {
		respond "ET API origin" 404
	}

	header {
		X-Content-Type-Options nosniff
		Referrer-Policy strict-origin-when-cross-origin
	}
}
